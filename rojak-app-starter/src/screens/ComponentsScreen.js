import React from 'react';
import {Text, StyleSheet, View} from 'react-native';

const ComponentsScreen = () => {
	const name = 'Stephen';
return (
	// insert header and subheader text here 
	);
};

const styles = StyleSheet.create ({
	// insert styling here
});

export default ComponentsScreen;